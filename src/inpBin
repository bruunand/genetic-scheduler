generation inp_bin_file(char *fileName)
{
    generation gen;
    
    inp = fopen(fileName, "r");
    
    while (inp != EOF)
    {
        fscanf("%ui")
    }
    
    fclose(inp);
    
    return gen;
}